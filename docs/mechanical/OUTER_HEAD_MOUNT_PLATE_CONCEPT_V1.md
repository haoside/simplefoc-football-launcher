# Outer Head Mount Plate Concept v1

## File

- `cad/assembly/p0-single-ball-launcher-v2/p0_single_ball_launcher_outer_rotor_parametric_v8.scad`

## Purpose

Advance the launcher-head concept from ring ears and link plates to an explicit outer mount-plate direction.

## Current concept chain

The current concept load path is now shown as:

- ring web sector
- support ear
- frame stub
- link plate
- outer mount plate

## What was added

- one outer mount plate concept in each strong web sector direction
- simple 4-hole visual pattern on each plate
- enough geometry to discuss where the launcher head would finally bolt into a larger side structure or outer frame bracket

## Placeholder values

| Item | Value |
|---|---:|
| outer mount plate width | `64mm` |
| outer mount plate height | `72mm` |
| outer mount plate thickness | `8mm` |
| outer mount plate hole pitch X/Y surrogate | `34 / 40mm` |
| outer mount plate hole diameter | `6.6mm` |

## Review focus

1. whether the head now reads as a plausible mountable subassembly
2. whether the mount-plate direction still aligns with the ring strong sectors
3. whether the current 6354 packaging remains favorable once final-side mounting surfaces are visible
